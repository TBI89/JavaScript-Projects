// Display the smaller number

let num1 = prompt("Enter the first number");
let num2 = prompt("Enter the second number");

if (num1 > num2) {
    document.write("The smaller number is: " + num2);
}

else {
    document.write("The smaller number is: " + num1);
}
